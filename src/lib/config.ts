import { dev } from '$app/environment';

export const title = 'Markdown Blog';
export const description = 'Elegant Markdown blog made with SvelteKit';

export const url = dev ? 'http://localhost:5173/' : 'url';
